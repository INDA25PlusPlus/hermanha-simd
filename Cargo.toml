[package]
name = "hermanha-simd"
version = "0.1.0"
edition = "2024"

[dependencies]
image = "0.25"

[dev-dependencies]
criterion = { version = "0.7", features = ["html_reports"] }

[toolchain]
channel = "nightly"

[[bench]]
name = "benchmark"
harness = false

